package com.missionhub.model;

import java.util.ArrayList;
import java.util.List;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;
import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.DaoConfig;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.Query;
import de.greenrobot.dao.QueryBuilder;
import de.greenrobot.dao.SqlUtils;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/**
 * DAO for table QUESTION.
 */
public class QuestionDao extends AbstractDao<Question, Long> {

	public static final String TABLENAME = "QUESTION";

	/**
	 * Properties of entity Question.<br/>
	 * Can be used for QueryBuilder and for referencing column names.
	 */
	public static class Properties {
		public final static Property Id = new Property(0, Long.class, "id", true, "_id");
		public final static Property Keyword_id = new Property(1, Long.class, "keyword_id", false, "KEYWORD_ID");
		public final static Property Label = new Property(2, String.class, "label", false, "LABEL");
		public final static Property Kind = new Property(3, String.class, "kind", false, "KIND");
		public final static Property Style = new Property(4, String.class, "style", false, "STYLE");
		public final static Property Required = new Property(5, Boolean.class, "required", false, "REQUIRED");
		public final static Property Active = new Property(6, Boolean.class, "active", false, "ACTIVE");
	};

	private DaoSession daoSession;

	private Query<Question> keyword_QuestionsQuery;

	public QuestionDao(final DaoConfig config) {
		super(config);
	}

	public QuestionDao(final DaoConfig config, final DaoSession daoSession) {
		super(config, daoSession);
		this.daoSession = daoSession;
	}

	/** Creates the underlying database table. */
	public static void createTable(final SQLiteDatabase db, final boolean ifNotExists) {
		final String constraint = ifNotExists ? "IF NOT EXISTS " : "";
		db.execSQL("CREATE TABLE " + constraint + "'QUESTION' (" + //
				"'_id' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
				"'KEYWORD_ID' INTEGER," + // 1: keyword_id
				"'LABEL' TEXT," + // 2: label
				"'KIND' TEXT," + // 3: kind
				"'STYLE' TEXT," + // 4: style
				"'REQUIRED' INTEGER," + // 5: required
				"'ACTIVE' INTEGER);"); // 6: active
	}

	/** Drops the underlying database table. */
	public static void dropTable(final SQLiteDatabase db, final boolean ifExists) {
		final String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'QUESTION'";
		db.execSQL(sql);
	}

	/** @inheritdoc */
	@Override
	protected void bindValues(final SQLiteStatement stmt, final Question entity) {
		stmt.clearBindings();

		final Long id = entity.getId();
		if (id != null) {
			stmt.bindLong(1, id);
		}

		final Long keyword_id = entity.getKeyword_id();
		if (keyword_id != null) {
			stmt.bindLong(2, keyword_id);
		}

		final String label = entity.getLabel();
		if (label != null) {
			stmt.bindString(3, label);
		}

		final String kind = entity.getKind();
		if (kind != null) {
			stmt.bindString(4, kind);
		}

		final String style = entity.getStyle();
		if (style != null) {
			stmt.bindString(5, style);
		}

		final Boolean required = entity.getRequired();
		if (required != null) {
			stmt.bindLong(6, required ? 1l : 0l);
		}

		final Boolean active = entity.getActive();
		if (active != null) {
			stmt.bindLong(7, active ? 1l : 0l);
		}
	}

	@Override
	protected void attachEntity(final Question entity) {
		super.attachEntity(entity);
		entity.__setDaoSession(daoSession);
	}

	/** @inheritdoc */
	@Override
	public Long readKey(final Cursor cursor, final int offset) {
		return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
	}

	/** @inheritdoc */
	@Override
	public Question readEntity(final Cursor cursor, final int offset) {
		final Question entity = new Question( //
				cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
				cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // keyword_id
				cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // label
				cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // kind
				cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // style
				cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0, // required
				cursor.isNull(offset + 6) ? null : cursor.getShort(offset + 6) != 0 // active
		);
		return entity;
	}

	/** @inheritdoc */
	@Override
	public void readEntity(final Cursor cursor, final Question entity, final int offset) {
		entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
		entity.setKeyword_id(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
		entity.setLabel(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
		entity.setKind(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
		entity.setStyle(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
		entity.setRequired(cursor.isNull(offset + 5) ? null : cursor.getShort(offset + 5) != 0);
		entity.setActive(cursor.isNull(offset + 6) ? null : cursor.getShort(offset + 6) != 0);
	}

	/** @inheritdoc */
	@Override
	protected Long updateKeyAfterInsert(final Question entity, final long rowId) {
		entity.setId(rowId);
		return rowId;
	}

	/** @inheritdoc */
	@Override
	public Long getKey(final Question entity) {
		if (entity != null) {
			return entity.getId();
		} else {
			return null;
		}
	}

	/** @inheritdoc */
	@Override
	protected boolean isEntityUpdateable() {
		return true;
	}

	/** Internal query to resolve the "questions" to-many relationship of Keyword. */
	public synchronized List<Question> _queryKeyword_Questions(final Long keyword_id) {
		if (keyword_QuestionsQuery == null) {
			final QueryBuilder<Question> queryBuilder = queryBuilder();
			queryBuilder.where(Properties.Keyword_id.eq(keyword_id));
			keyword_QuestionsQuery = queryBuilder.build();
		} else {
			keyword_QuestionsQuery.setParameter(0, keyword_id);
		}
		return keyword_QuestionsQuery.list();
	}

	private String selectDeep;

	protected String getSelectDeep() {
		if (selectDeep == null) {
			final StringBuilder builder = new StringBuilder("SELECT ");
			SqlUtils.appendColumns(builder, "T", getAllColumns());
			builder.append(',');
			SqlUtils.appendColumns(builder, "T0", daoSession.getKeywordDao().getAllColumns());
			builder.append(" FROM QUESTION T");
			builder.append(" LEFT JOIN KEYWORD T0 ON T.'KEYWORD_ID'=T0.'_id'");
			builder.append(' ');
			selectDeep = builder.toString();
		}
		return selectDeep;
	}

	protected Question loadCurrentDeep(final Cursor cursor, final boolean lock) {
		final Question entity = loadCurrent(cursor, 0, lock);
		final int offset = getAllColumns().length;

		final Keyword keyword = loadCurrentOther(daoSession.getKeywordDao(), cursor, offset);
		entity.setKeyword(keyword);

		return entity;
	}

	public Question loadDeep(final Long key) {
		assertSinglePk();
		if (key == null) {
			return null;
		}

		final StringBuilder builder = new StringBuilder(getSelectDeep());
		builder.append("WHERE ");
		SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
		final String sql = builder.toString();

		final String[] keyArray = new String[] { key.toString() };
		final Cursor cursor = db.rawQuery(sql, keyArray);

		try {
			final boolean available = cursor.moveToFirst();
			if (!available) {
				return null;
			} else if (!cursor.isLast()) {
				throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
			}
			return loadCurrentDeep(cursor, true);
		} finally {
			cursor.close();
		}
	}

	/** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
	public List<Question> loadAllDeepFromCursor(final Cursor cursor) {
		final int count = cursor.getCount();
		final List<Question> list = new ArrayList<Question>(count);

		if (cursor.moveToFirst()) {
			if (identityScope != null) {
				identityScope.lock();
				identityScope.reserveRoom(count);
			}
			try {
				do {
					list.add(loadCurrentDeep(cursor, false));
				} while (cursor.moveToNext());
			} finally {
				if (identityScope != null) {
					identityScope.unlock();
				}
			}
		}
		return list;
	}

	protected List<Question> loadDeepAllAndCloseCursor(final Cursor cursor) {
		try {
			return loadAllDeepFromCursor(cursor);
		} finally {
			cursor.close();
		}
	}

	/** A raw-style query where you can pass any WHERE clause and arguments. */
	public List<Question> queryDeep(final String where, final String... selectionArg) {
		final Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
		return loadDeepAllAndCloseCursor(cursor);
	}

}
