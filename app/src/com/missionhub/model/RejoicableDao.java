package com.missionhub.model;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table REJOICABLE.
*/
public class RejoicableDao extends AbstractDao<Rejoicable, Long> {

    public static final String TABLENAME = "REJOICABLE";

    /**
     * Properties of entity Rejoicable.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property What = new Property(1, String.class, "what", false, "WHAT");
        public final static Property Person_id = new Property(2, Long.class, "person_id", false, "PERSON_ID");
        public final static Property Created_by_id = new Property(3, Long.class, "created_by_id", false, "CREATED_BY_ID");
        public final static Property Followup_comment_id = new Property(4, Long.class, "followup_comment_id", false, "FOLLOWUP_COMMENT_ID");
        public final static Property Organization_id = new Property(5, Long.class, "organization_id", false, "ORGANIZATION_ID");
        public final static Property Updated_at = new Property(6, java.util.Date.class, "updated_at", false, "UPDATED_AT");
        public final static Property Created_at = new Property(7, java.util.Date.class, "created_at", false, "CREATED_AT");
    };

    private DaoSession daoSession;

    private Query<Rejoicable> person_RejoicableListQuery;
    private Query<Rejoicable> followupComment_RejoicablesQuery;

    public RejoicableDao(DaoConfig config) {
        super(config);
    }
    
    public RejoicableDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'REJOICABLE' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'WHAT' TEXT," + // 1: what
                "'PERSON_ID' INTEGER," + // 2: person_id
                "'CREATED_BY_ID' INTEGER," + // 3: created_by_id
                "'FOLLOWUP_COMMENT_ID' INTEGER," + // 4: followup_comment_id
                "'ORGANIZATION_ID' INTEGER," + // 5: organization_id
                "'UPDATED_AT' INTEGER," + // 6: updated_at
                "'CREATED_AT' INTEGER);"); // 7: created_at
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'REJOICABLE'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Rejoicable entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String what = entity.getWhat();
        if (what != null) {
            stmt.bindString(2, what);
        }
 
        Long person_id = entity.getPerson_id();
        if (person_id != null) {
            stmt.bindLong(3, person_id);
        }
 
        Long created_by_id = entity.getCreated_by_id();
        if (created_by_id != null) {
            stmt.bindLong(4, created_by_id);
        }
 
        Long followup_comment_id = entity.getFollowup_comment_id();
        if (followup_comment_id != null) {
            stmt.bindLong(5, followup_comment_id);
        }
 
        Long organization_id = entity.getOrganization_id();
        if (organization_id != null) {
            stmt.bindLong(6, organization_id);
        }
 
        java.util.Date updated_at = entity.getUpdated_at();
        if (updated_at != null) {
            stmt.bindLong(7, updated_at.getTime());
        }
 
        java.util.Date created_at = entity.getCreated_at();
        if (created_at != null) {
            stmt.bindLong(8, created_at.getTime());
        }
    }

    @Override
    protected void attachEntity(Rejoicable entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Rejoicable readEntity(Cursor cursor, int offset) {
        Rejoicable entity = new Rejoicable( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // what
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // person_id
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3), // created_by_id
            cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4), // followup_comment_id
            cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5), // organization_id
            cursor.isNull(offset + 6) ? null : new java.util.Date(cursor.getLong(offset + 6)), // updated_at
            cursor.isNull(offset + 7) ? null : new java.util.Date(cursor.getLong(offset + 7)) // created_at
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Rejoicable entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setWhat(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setPerson_id(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setCreated_by_id(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
        entity.setFollowup_comment_id(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
        entity.setOrganization_id(cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5));
        entity.setUpdated_at(cursor.isNull(offset + 6) ? null : new java.util.Date(cursor.getLong(offset + 6)));
        entity.setCreated_at(cursor.isNull(offset + 7) ? null : new java.util.Date(cursor.getLong(offset + 7)));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Rejoicable entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Rejoicable entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "rejoicableList" to-many relationship of Person. */
    public List<Rejoicable> _queryPerson_RejoicableList(Long created_by_id) {
        synchronized (this) {
            if (person_RejoicableListQuery == null) {
                QueryBuilder<Rejoicable> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.Created_by_id.eq(null));
                person_RejoicableListQuery = queryBuilder.build();
            }
        }
        Query<Rejoicable> query = person_RejoicableListQuery.forCurrentThread();
        query.setParameter(0, created_by_id);
        return query.list();
    }

    /** Internal query to resolve the "rejoicables" to-many relationship of FollowupComment. */
    public List<Rejoicable> _queryFollowupComment_Rejoicables(Long followup_comment_id) {
        synchronized (this) {
            if (followupComment_RejoicablesQuery == null) {
                QueryBuilder<Rejoicable> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.Followup_comment_id.eq(null));
                followupComment_RejoicablesQuery = queryBuilder.build();
            }
        }
        Query<Rejoicable> query = followupComment_RejoicablesQuery.forCurrentThread();
        query.setParameter(0, followup_comment_id);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPersonDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getPhoneNumberDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getFollowupCommentDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T3", daoSession.getOrganizationDao().getAllColumns());
            builder.append(" FROM REJOICABLE T");
            builder.append(" LEFT JOIN PERSON T0 ON T.'PERSON_ID'=T0.'_id'");
            builder.append(" LEFT JOIN PHONE_NUMBER T1 ON T.'CREATED_BY_ID'=T1.'_id'");
            builder.append(" LEFT JOIN FOLLOWUP_COMMENT T2 ON T.'FOLLOWUP_COMMENT_ID'=T2.'_id'");
            builder.append(" LEFT JOIN ORGANIZATION T3 ON T.'ORGANIZATION_ID'=T3.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Rejoicable loadCurrentDeep(Cursor cursor, boolean lock) {
        Rejoicable entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Person person = loadCurrentOther(daoSession.getPersonDao(), cursor, offset);
        entity.setPerson(person);
        offset += daoSession.getPersonDao().getAllColumns().length;

        PhoneNumber phoneNumber = loadCurrentOther(daoSession.getPhoneNumberDao(), cursor, offset);
        entity.setPhoneNumber(phoneNumber);
        offset += daoSession.getPhoneNumberDao().getAllColumns().length;

        FollowupComment followupComment = loadCurrentOther(daoSession.getFollowupCommentDao(), cursor, offset);
        entity.setFollowupComment(followupComment);
        offset += daoSession.getFollowupCommentDao().getAllColumns().length;

        Organization organization = loadCurrentOther(daoSession.getOrganizationDao(), cursor, offset);
        entity.setOrganization(organization);

        return entity;    
    }

    public Rejoicable loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Rejoicable> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Rejoicable> list = new ArrayList<Rejoicable>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Rejoicable> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Rejoicable> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
